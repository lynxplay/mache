--- a/net/minecraft/client/renderer/blockentity/BeaconRenderer.java
+++ b/net/minecraft/client/renderer/blockentity/BeaconRenderer.java
@@ -118,10 +_,16 @@
         float uu2 = 1.0F;
         float vv2 = -1.0F + texVOff;
         float vv1 = height * scale * (0.5F / solidBeamRadius) + vv2;
+        float finalWnz = wnz;
+        float finalEnx = enx;
+        float finalWsx1 = wsx;
+        float finalEsz = esz;
+        float finalVv2 = vv1;
+        float finalVv3 = vv2;
         submitNodeCollector.submitCustomGeometry(
             poseStack,
             RenderTypes.beaconBeam(beamLocation, false),
-            (pose, buffer) -> renderPart(pose, buffer, color, beamStart, beamEnd, 0.0F, wnz, enx, 0.0F, wsx, 0.0F, 0.0F, esz, 0.0F, 1.0F, vv1, vv2)
+            (pose, buffer) -> renderPart(pose, buffer, color, beamStart, beamEnd, 0.0F, finalWnz, finalEnx, 0.0F, finalWsx1, 0.0F, 0.0F, finalEsz, 0.0F, 1.0F, finalVv2, finalVv3)
         );
         poseStack.popPose();
         wnx = -beamGlowRadius;
@@ -136,10 +_,25 @@
         uu2 = 1.0F;
         vv2 = -1.0F + texVOff;
         vv1 = height * scale + vv2;
+        float finalWnx = wnx;
+        float finalEnz = enz;
+        float finalWsx = wsx;
+        float finalVv = vv1;
+        float finalVv1 = vv2;
+        float finalWnx1 = wnx;
+        float finalWnz1 = wnz;
+        float finalEnx1 = enx;
+        float finalEnz1 = enz;
+        float finalWsx2 = wsx;
+        float finalWsz = wsz;
+        float finalEsx = esx;
+        float finalEsz1 = esz;
+        float finalVv4 = vv1;
+        float finalVv5 = vv2;
         submitNodeCollector.submitCustomGeometry(
             poseStack,
             RenderTypes.beaconBeam(beamLocation, true),
-            (pose, buffer) -> renderPart(pose, buffer, ARGB.color(32, color), beamStart, beamEnd, wnx, wnz, enx, enz, wsx, wsz, esx, esz, 0.0F, 1.0F, vv1, vv2)
+            (pose, buffer) -> renderPart(pose, buffer, ARGB.color(32, color), beamStart, beamEnd, finalWnx1, finalWnz1, finalEnx1, finalEnz1, finalWsx2, finalWsz, finalEsx, finalEsz1, 0.0F, 1.0F, finalVv4, finalVv5)
         );
         poseStack.popPose();
     }
